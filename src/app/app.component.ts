import { Component } from '@angular/core';

@Component({
  selector: 'app-root', //HTML tag used to embed the component
  templateUrl: './app.component.html', //HTML file we are using
  styleUrls: ['./app.component.css'] //can have more than 1
})

export class AppComponent { //Put any properties of the component
}
